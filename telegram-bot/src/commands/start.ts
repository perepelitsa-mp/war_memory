/**
 * =====================================================
 * Команда /start
 * =====================================================
 */

import { Context } from 'telegraf';

export async function startCommand(ctx: Context) {
  const message = `
👋 *Добро пожаловать в бот "Книга Памяти Кавалерово"!*

Этот бот отправляет уведомления о важных событиях:
• Модерация ваших карточек
• Новые воспоминания к вашим карточкам
• Новые события в таймлайне
• Новые комментарии

Чтобы подписаться на уведомления, используйте команду:
/subscribe <ваш email>

Например:
\`/subscribe ivan@example.com\`

После этого вы будете получать уведомления в этот чат.

Для получения списка всех команд используйте /help
`;

  await ctx.reply(message, { parse_mode: 'Markdown' });
}
